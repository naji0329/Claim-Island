import { Html } from "@react-three/drei";

export const Loading3DView = () => (
  <Html>
    <div style={{
      background: "#202035",
      color: "white",
      padding: "10px 15px",
      borderRadius: "5px",
      transform: "translate(-50%, -50%)"
    }}>
      LOADING
    </div>
  </Html>
);
